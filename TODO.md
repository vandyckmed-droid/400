# TODO

Small deferred items. One line each, no dates, no priorities. Cross or delete a line when it is
done; the README stays the source of truth for how the product works.

- Bars folder is rewritten daily, so repo history grows ~19 MB/day. Evaluate: commit diffs only,
  move bars to GitHub Release assets or object storage, or periodic history squash.
- Older cross-sections have survivorship bias because departed names are dropped. Fix depends on
  the storage decision above.
- Within-sector and sector-relative rankings have never been backtested.
